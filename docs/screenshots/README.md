# Add screenshots here after running the app

Suggested captures for README:

1. **Tasks view** — group list, task table, output panel
2. **Settings** — configuration status (green checks)
3. **Dashboard** — success rate hero + sparkline
4. **Preflight modal** — live run confirmation

Run `npm run dev`, use the **AutoRSA Desktop** Electron window (not localhost:5173 in a browser).
